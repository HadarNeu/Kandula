apiVersion: 1

providers:
  - name: "Default"
    folder: "Kandula"
    updateIntervalSeconds: 10
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards